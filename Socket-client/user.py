class User:
    def __init__(self, userName):
        self.name = userName
     
     
    # def sendMessage(self, chatMessage):
    #     if(chatMessage.user.address != self.address):
    #         self.listen.sendAll(chatMessage)
